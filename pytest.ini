[pytest]
DJANGO_SETTINGS_MODULE=restaurant.settings
junit_family=xunit1
addopts=
    --junitxml=build/report.xml
    --cov=.
    --cov-config .coveragerc
    --cov-report term
    --cov-report xml
    --verbose
    --capture=sys
    --exitfirst
testpaths=.
